<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/home">

    <fragment
        android:id="@+id/home"
        android:name="myproject.quickgrocer.User.UserHome"
        android:label="@string/menu_home"
        tools:layout="@layout/activity_list" />

    <fragment
        android:id="@+id/checkout"
        android:name="myproject.quickgrocer.User.Checkout"
        android:label="Checkout"
        tools:layout="@layout/activity_checkout" />

    <fragment
        android:id="@+id/about"
        android:name="myproject.quickgrocer.User.AboutFragment"
        android:label="About"
        tools:layout="@layout/fragment_about"/>

    <activity
        android:id="@+id/logout"
        android:name="myproject.quickgrocer.MainActivity"
        tools:layout="@layout/activity_main" />


</navigation>
